step by step 
Create a folder called youtube
Within youtube folder used vite@ library ( node.js must be installed)
Deleted some unecessary files 
inside src was created a pages folder (to storage our pages)
install npm i sass  ( style aligment, cool features to style)
create a folder and file inside src/style/global.scss ( it's .scss instead of .css because of the sass)
#tip - install material extension to create icon on your explorer ( cool)
downloaded poppins font from fonts.google and ( 400,500, 700bold) and added it before the title on index.html
